services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "8083:4000"

    env_file:
      - .env

    networks:
      - react-job-portal-network

networks:
  react-job-portal-network:
    external: true